---
molecule:
  raw_env_vars:
    ANSIBLE_LIBRARY: ../library

  ignore_paths:
    - .git
    - .vagrant
    - .molecule
    - venv

docker:
  containers:
    - name: logrotate-01
      image: ubuntu
      image_version: latest
